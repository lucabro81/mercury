---
"mercury": patch
---

- Build the `runCommand` tool's usage example from the CLIs actually enabled on the instance instead of a hardcoded one, so the example never names a CLI that isn't configured.
