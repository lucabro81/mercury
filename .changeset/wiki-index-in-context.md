---
"mercury": minor
---

Every new conversation now starts with the wiki's own index already in view, instead of relying on Mercury deciding on its own to go look. This makes it more likely relevant existing documentation actually gets used instead of re-discovered from scratch.
