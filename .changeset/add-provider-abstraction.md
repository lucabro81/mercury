---
"mercury": minor
---

Introduce a shared Provider interface so every communication channel (terminal, Google Chat, and any future one) runs through the same turn-handling and proactive-notification logic instead of each channel duplicating it.
