---
"mercury": patch
---

Removed the tools for sending a Google Chat notification to a user or the admin space by email — the lookup they depended on could never resolve a real user on any actual deployment, so neither ever worked.
