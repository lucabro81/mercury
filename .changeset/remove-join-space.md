---
"mercury": patch
---

Removed the tool that let Mercury try to add itself to a Google Chat space on request — it never actually worked (silently did nothing while claiming success), so the capability is gone rather than fixed.
