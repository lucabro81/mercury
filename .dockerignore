# Build context is the monorepo root (see apps/mercury/docker-compose.yml):
# Docker only reads a .dockerignore from the context root, so this is the one
# that applies, not apps/mercury/.dockerignore.
node_modules
.git
.turbo
.claude
CLAUDE.local.md
docs
bin/
*.log
.env
.env.*
!.env.example
